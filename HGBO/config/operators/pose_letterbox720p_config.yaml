operator: PoseLetterbox720p
op_type: vector
description: "720p BGR uint8 to 640x640 BGR uint8 fixed-geometry letterbox"

input_shape: [720, 1280, 3]
output_shape: [640, 640, 3]
dtype: uint8

# Every search parameter below is consumed by formal host tiling data. Do not
# add pipeline or alignment fields until the kernel consumes them.
direct_search: true

op_profile:
  total_elements: 2764800
  estimated_ops_per_element: 8
  min_work_per_core: 256
